# 首页键盘收起设计

## 目标

首页输入 AppKey 或 Token 后，点击页面空白区域或“进入游戏”按钮时收起键盘。

## 方案

- 在 `ViewController` 根视图添加点击手势。
- 手势只响应非控件区域，避免改变输入框、展示高度按钮等控件的原有行为。
- 手势不取消触摸事件，保留滚动视图原有滚动能力。
- 在 `enterGame()` 开头主动结束编辑，确保按钮点击后立即收起键盘。

## 验证

- 检查差异只包含上述交互所需代码。
- 使用 iOS 模拟器目标执行无签名构建。
- 运行时仍需在模拟器确认空白点击与按钮点击的实际键盘表现。
