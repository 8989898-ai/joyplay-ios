# Game ID 1 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Make every game URL use the fixed query parameter `gameId=1`.

**Architecture:** Keep the existing `GameURLBuilder` API and URL structure. Change only the fixed query value and its focused test assertion.

**Tech Stack:** Swift, Foundation, UIKit project, `swiftc`, `xcodebuild`

---

### Task 1: Fix the game ID query parameter

**Files:**
- Modify: `Tests/main.swift:82`
- Modify: `joyplay-ios/GameConfiguration.swift:119`

**Step 1: Write the failing test**

Change the existing assertion to:

```swift
expect(queryItems["gameId"] == "1", "gameId 应固定为 1")
```

**Step 2: Run test to verify it fails**

Run the focused `swiftc` test. Expected: failure stating `gameId 应固定为 1`.

**Step 3: Write minimal implementation**

Change the `gameId` query item value from `"2"` to `"1"`.

**Step 4: Verify**

Run the focused test, `git diff --check`, and an unsigned Simulator build with a fresh DerivedData path. Expected: all commands exit successfully.
