# 游戏页 WebView 底部对齐设计

## 目标

游戏页使用半屏或大半屏展示时，WebView 贴近安全区底部；全屏展示保持覆盖整个安全区。

## 设计

- 保留现有左右边缘约束和按安全区高度计算的比例高度。
- 将 WebView 的顶部约束替换为安全区底部约束。
- 三种展示模式共用同一套约束；全屏高度等于安全区高度，因此视觉行为不变。

## 验证

- 检查差异，确认仅改变相关布局约束。
- 执行无签名 Xcode 构建，确认约束代码可编译。
- 运行时确认半屏和大半屏贴安全区底部、全屏布局不变。
