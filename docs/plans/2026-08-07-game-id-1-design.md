# Game ID 固定为 1 设计

## 目标

所有游戏展示模式生成的 URL 都固定使用 `gameId=1`。

## 设计

仅修改 `GameURLBuilder` 的固定查询参数，并同步现有聚焦测试断言。保留 `appKey`、`token`、域名、路径和各模式的 `mini=0/1/2` 映射，不引入新常量或可配置参数。

## 验证

先把测试期望改为 `1` 并确认其因当前实现返回 `2` 而失败，再修改实现并确认聚焦测试及无签名 Simulator 构建通过。
