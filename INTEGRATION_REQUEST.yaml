# This file contains optional business overrides and records the integration; it is not a prerequisite form.
# null means unspecified: AI automatically scans the target project and asks only about business decisions it cannot uniquely determine.
# After integration, AI may record automatically discovered or user-confirmed choices here for review and later maintenance.
project_path: null
project_kind: null
scheme: null
app_target: null
ui_framework: UIKit
minimum_ios: '15.6'

modes:
  full:
    enabled: null
    host_controller: null
    close_behavior: null
  half:
    enabled: null
    host_controller: null
    close_behavior: null
  large_half:
    enabled: null
    host_controller: null
    close_behavior: null

game_url:
  game_url_source: null
  invalid_url_behavior: null
  requires_https: true
  backend_must_omit_safe_top: true
  backend_must_omit_padding_bottom: true
  client_appends_safe_top_for_full_screen: true
  client_appends_padding_bottom_for_full_screen: true

layout:
  width_height_ratio_source: null
  invalid_ratio_behavior: null

recharge:
  ui_owner: host
  recharge_handler: null
  notify_game_after_success: true

validation:
  configuration: Debug
  destination: generic/platform=iOS Simulator
